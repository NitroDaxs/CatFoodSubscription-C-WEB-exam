@model CatFoodSubscription.Web.ViewModels.Product.ProductDetailViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/Product/styles.css" />
    <title>Product Details</title>
</head>
<body>

    <h1>Product Details</h1>

    <div>
        <img src="@Model.ImageUrl" alt="Product Image" class="product-image">
        <h2>@Model.Name</h2>
        <p>Description: @Model.Description</p>
        <p>Price: $@Model.Price</p>
        <p>Category: @Model.Category</p>

        <form asp-controller="Order" asp-action="AddToCart" method="post" onsubmit="return addToCart()">
            <input type="hidden" name="productId" value="@Model.Id" />
            <button type="submit" name="purchaseType" value="false">Add to Cart</button>
        </form>
    </div>
</body>
</html>